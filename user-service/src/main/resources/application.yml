server:
  port: 8081
  servlet:
    context-path: /user-service

spring:
  application:
    name: user-service
  thymeleaf:
    prefix: classpath:/templates/
    suffix: .html
    mode: HTML
    cache: false

  datasource:
    url: jdbc:postgresql://localhost:5432/faceclone
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver

  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update
    show-sql: true
  kafka:
    topic:
      forget-password-send-mail: forget-password-send-mail
    group-id:
      forget-password-send-mail: 1
  mail:
    host: smtp.gmail.com
    port: 587
    username: space.kayx1@gmail.com
    password: sjhh snag ubcf ebvy
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html
    enabled: true

#  data:
#    redis:
#      host: localhost
#      port: 6379
#
#  cache:
#    type: redis

#logging:
#  level:
#    org.springframework: DEBUG
#    com.userservice: DEBUG

#eureka:
#  client:
#    service-url:
#      defaultZone: http://localhost:8761/eureka
